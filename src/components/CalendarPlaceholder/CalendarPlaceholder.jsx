export function CalendarPlaceholder() {
  return (
    <div style={{
      padding: '28px 20px',
      textAlign: 'center',
      color: 'rgba(255, 255, 255, 0.4)',
      fontSize: '0.85rem',
      fontWeight: 500,
      letterSpacing: '-0.01em',
    }}>
      Google Calendar — coming in v2
    </div>
  );
}
